# Known failing / vacuous gates.
#
# One gate ID per line. '#' starts a comment. sim/gate.py tolerates exactly
# these and blocks on anything else.
#
# This list is DEBT, not permission. A gate belongs here only with a written
# reason and only while someone intends to fix it. Adding a line here to make
# a red suite go green is the same offence as loosening a threshold.
#
# Baseline 2026-08-28, after the handoff audit added M4B:
# 25 gates, 5 FAIL, 1 VACUOUS, 19 pass.  Verified by a --tier full run.
#
# Every figure quoted in the reasons below is from a --tier full run on
# 28 August 2026. They were stale once: this file carried S2b at -14.51,
# S2c at +24.04, S2_LEGACY at -0.40 and S1_PD at ECE 0.040, none of which
# the suite produces. If you change anything, re-run the suite and update
# these numbers in the same commit -- a reason file that disagrees with the
# gate it explains is worse than no reason file.

# ---------------------------------------------------------------------------
S1
# belief calibration: ECE<0.10 and monotone.  Currently ECE=0.091, monotone=False.
#
# THE 0.10 ECE THRESHOLD WAS DECLARED IN docs/05_TEST_DESIGN.md BEFORE ANY
# RESULTS WERE SEEN. IT MUST NOT BE LOOSENED. Note that ECE is already inside
# 0.10 -- the gate fails on the MONOTONICITY half, i.e. the reliability curve
# is not ordered. Raising the ECE bound would not even fix it; it would only
# hide which half is broken. The bins show the belief is badly underconfident
# in the low-probability range (predicted 0.011, actual 0.500 at n=28) and
# overconfident at the top (predicted 0.998, actual 0.919 at n=5873).
# Unchanged by the 27 August rebuild. Untouched, still the one failure the
# handoff declared.

# ---------------------------------------------------------------------------
M1
# "5th attempt in a cycle" mutant.  Status: VACUOUS at BOTH pe=1 and pe=7.
#
# Diagnosed 2026-08-27: the mutant works, but the attempt cap is never the
# binding constraint. The deepest any mandate-cycle reaches is 3 attempts at
# pe=1 and 4 at pe=7, against NPCI_MAX=4, so the 5th attempt that would trip
# the counter never happens. Moving the gate to the contended operating point
# was tried and did NOT fix it -- this was predicted in advance, see the
# pre-registration in NOTES.md.
#
# Consequence, unchanged: the cap-violation counter in sim/harness.py
# (`if ledger[lk] >= cap:`) is the ONLY detector of a 5th attempt, M1 is the
# only gate that proves that detector works, and T7's cap clause reads the
# same counter -- so both cap gates are exactly as strong as M1, i.e. unproven.
# Do not quote the NPCI attempt-cap compliance claim until this is fixed.
#
# Next thing to try (NOT yet done, needs sign-off): run the mutant with
# cap_override=2, where the cap does bind at this operating point. That tests
# the counter mechanism rather than the NPCI-specific value of 4.

# ---------------------------------------------------------------------------
S2b
# "placebo_pd is neutral vs own_pd".  Currently -14.09 pts (+/-2.09). NOT neutral.
#
# This is a finding about the experiment design, not a code defect, and it is
# the most important thing the 27 August rebuild produced.
#
# solo_placebo does not inject NEUTRAL extra update events. It injects WRONG
# ones -- outcomes computed against a different customer's balance
# (sim/harness.py:312). Feeding a belief actively misleading observations is
# worse than feeding it nothing, so the placebo arm is damaged rather than
# merely uninformative. Predicted in advance (NOTES.md pre-registration).
#
# Why it matters: S2c (real - placebo) is algebraically S2a + |S2b|, an
# identity for paired means, not an independent measurement. 60% of the
# +23.62 headline is placebo damage. docs/02_RESULTS.md used to present that
# number as "the strongest evidence we have" that pooling is information.
# It is not. The honest moat number is S2a, +9.53 pts (+/-1.81).
#
# Fixing this means building a control that matches the UPDATE COUNT without
# supplying wrong information -- e.g. label-shuffled observations with the
# matched base rate -- not relaxing the neutrality threshold. Left failing on
# purpose so the confound stays visible.

# ---------------------------------------------------------------------------
S2_LEGACY
# "point-estimate pooling vs placebo".  Currently -0.38 pts (+/-0.22).
#
# RETIRED ARCHITECTURE. This is the original S2, unchanged, still running the
# point-estimate payday trio (solo_shared / solo_placebo / solo_pop) at the
# uncontended pe=1 operating point. docs/02_RESULTS.md already reports the
# point-estimate pooling effect as -0.16/-0.49 (n.s.), so this gate is
# faithfully reproducing a documented null.
#
# It is kept, failing, ON PURPOSE. The S2 rewrite replaced a red gate with
# three new ones; deleting the red gate at the same time would have been
# indistinguishable from loosening a test to get green. Keeping it visible is
# what makes the change auditable.
#
# Do not "fix" this by deleting it. If the point-estimate architecture is ever
# formally removed from the harness, this gate goes with it and not before.

# ---------------------------------------------------------------------------
S1_PD
# "SHIPPING belief calibration: ECE<0.10 and monotone". ECE=0.026, monotone=False.
#
# ADDED 2026-08-28, and the reason it exists is the finding.
#
# S1 runs `portfolio`. `portfolio` does not end in "_pd", so it carries
# w3.Belief -- the POINT-ESTIMATE payday filter. The policy this project
# recommends is `solo_shared_pd`, which carries w3.BeliefPD. So for the entire
# life of the project the calibration gate has been measuring a filter that is
# not the product, and every conclusion drawn from S1 about "the belief
# filter" was about the wrong object.
#
# S1 was NOT repointed. It is a pre-registered gate whose threshold was
# declared in docs/05_TEST_DESIGN.md before any result was seen, and quietly
# aiming it at a different policy would be indistinguishable from moving a
# test until it says something else. S1_PD is an ADDITION with the IDENTICAL
# threshold, on the real filter.
#
# It fails, on the monotonicity half, exactly as predicted in advance (the
# pre-registration guessed ECE 0.04-0.07 and "still fails on monotonicity",
# at ~60% confidence).
#
# TWO DIFFERENT ECE NUMBERS EXIST FOR THIS FILTER AND THEY ARE NOT THE SAME
# MEASUREMENT. This gate reports ECE=0.026 on populations Pc0-Pc2 (n=120,
# pop seeds 300-302, run seeds 800-802). sim/fair_audit.py reports 0.059
# shipped -> 0.040 fitted on DIFFERENT populations (n=100, pop seeds
# 700-702, run seeds 1600-1602). This file used to quote fair_audit's 0.040
# as if it were the gate's. Quote 0.026 for S1_PD and name fair_audit when
# quoting 0.040. Fitting improved ECE on both and made neither monotone.
#
# The remaining break is in the mid-range, and the plausible causes
# are the two things the fit does not touch: the filter does not model the
# balance floor at zero, and it approximates the world's hourly U(0.4,1.6)
# spend jitter with a fixed 3-tap kernel.
#
# Do NOT loosen the 0.10 bound; ECE is already well inside it. The gate fails
# on monotonicity and raising the ECE bound would only hide which half is
# broken.

# ---------------------------------------------------------------------------
M4B
# "no mutant writes the counter it is graded on".
# Currently FAIL: M4(pending->V.pending), M5(represent->V.represent).
#
# ADDED 2026-08-28 by the handoff audit. This is the fourth time this project
# has shipped a guardrail that passed by construction, and the first time the
# guardrail was a MUTATION test -- the thing whose entire job is to be
# adversarial.
#
# M1-M5 each run a mutant and require vdetail[field] to move. That is evidence
# only if the mutant creates an illegal STATE and a DIFFERENT piece of code
# notices. Two of the five increment the counter themselves:
#
#   sim/harness.py:610-612   mutate == "pending"   -> V.pending += 1
#   sim/harness.py:331-333   mutate == "represent" -> V.represent += 1
#
# Measured on an instrumented copy of the harness (portfolio, pop P, seed 7):
#
#   mutate='pending'    1066 counted, 1066 self-written, 0 independent.
#                       The independent branch is `if m["pend"] is not None`
#                       at harness.py:607, and `live` at harness.py:349-351
#                       has ALREADY filtered `m["pend"] is None`, so it is
#                       unreachable for every policy. M4 IS VACUOUS and has
#                       been reporting PASS.
#   mutate='represent'   608 counted, 304 self-written, 304 independent.
#                       M5 still binds -- the dispatch-time check at
#                       harness.py:262 finds it -- but the reported count is
#                       inflated 2x.
#
# CONSEQUENCE, and it is the same shape as M1's: the one-pending-notification
# constraint has NO working test. Do not put pending-notification compliance
# in the pitch or the architecture doc, for exactly the reason the attempt cap
# is already banned. Two of the five Stage 0 rules are now unproven.
#
# WHY IT IS NOT FIXED HERE. The fix is in sim/harness.py -- delete the two
# self-writes and make the pending check independent of `live` -- and
# sim/harness.py is FROZEN until 5 September (CLAUDE.md, tag `model-frozen`).
# Removing the self-writes would also change M4/M5's reported counts, which
# would move gate T9's reference. That is a model change, not a doc fix, and
# it needs sign-off from Tanmay.
#
# WHEN THE FREEZE LIFTS, in this order:
#   1. delete `V.pending += 1` from the mutate branch (harness.py:612) and
#      `V.represent += 1` from harness.py:333.
#   2. give `pending` a detector that `live` cannot pre-satisfy -- count at
#      dispatch, from the harness's own record, the way cap/peak/lead do.
#   3. re-run M4. If it now reports VACUOUS, that is the TRUE state and it
#      belongs in this file under its own heading, not hidden by a self-write.
#   4. re-baseline T9 with sim/t9_reference.py --recapture and paste the diff
#      into NOTES.md.
#
# Do NOT "fix" M4B by deleting it, by exempting a mutant, or by making the
# detector stop looking at `represent`. It reports VACUOUS if it ever flags
# all five, which is its own falsifiability check.

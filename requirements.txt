numpy==2.4.2          # the 27-gate suite and the agent need only this
pyyaml==6.0.3         # agent/eval/ reads golden_cases.yaml
lightgbm==4.6.0       # sim/ml_study.py, sim/ml_diagnose.py -- the ML baseline
scikit-learn==1.8.0   # same

# Verified on CPython 3.12.0, Windows. `python sim/gate.py --tier full` imports
# nothing below the first line. Notes on rebuilding the ML artifacts:
# docs/results.md.
